{
  "name": "yt-link",
  "version": "0.1.0",
  "description": "Cross-platform application to download and manage your favorite videos.",
  "main": "electron-app/main.js",
  "homepage": "./",
  "scripts": {
    "start": "(cd frontend && npm install && npm run build) && electron .",
    "build": "(cd frontend && npm install && npm run build) && electron-builder",
    "build:backend": "npm install --prefix backend",
    "build:frontend": "npm install --prefix frontend && npm run build --prefix frontend",
    "dist": "electron-builder"
  },
  "keywords": [
    "youtube",
    "downloader",
    "electron"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "electron": "^28.2.3",
    "electron-builder": "^24.13.3"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "electron-updater": "^6.1.8",
    "express": "^4.19.2"
  },
  "build": {
    "appId": "com.steven.ytlink",
    "productName": "YT Link",
    "directories": {
      "output": "dist"
    },
    "files": [
      "electron-app/**/*",
      "frontend/out/**/*",
      "package.json"
    ],
    "win": {
      "target": "nsis"
    },
    "mac": {
      "target": "dmg"
    }
  }
}
