name: Build & Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build-and-release:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest, windows-latest]
    
    permissions:
      contents: write

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'frontend/package-lock.json'
      
      - name: Install ffmpeg (for both OS)
        id: install_ffmpeg # Assign an ID to this step
        uses: eugenmayer/setup-ffmpeg@v2
      
      - name: Create Python virtual environment and install dependencies
        run: |
          cd service
          python -m venv venv
          # This robust command works for both Windows and Mac shells
          ./venv/bin/python -m pip install -r requirements.txt || ./venv/Scripts/python.exe -m pip install -r requirements.txt
          ./venv/bin/python -m pip install pyinstaller || ./venv/Scripts/python.exe -m pip install pyinstaller
        shell: bash

      - name: Build Python Backend EXE (Windows only)
        if: matrix.os == 'windows-latest'
        run: |
          cd service
          # This command now includes the path to ffmpeg.exe for bundling
          ./venv/Scripts/python.exe -m PyInstaller --onefile --noconsole --clean \
            --add-binary "./venv/Scripts/yt-dlp.exe;." \
            --add-binary "${{ steps.install_ffmpeg.outputs.ffmpeg-path }};." \
            app.py
        shell: bash

      - name: Install Frontend Dependencies
        run: npm install
        working-directory: ./frontend

      - name: Build and Package Electron App
        run: npm run electron:build -- --publish always
        working-directory: ./frontend
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          IS_ELECTRON: 'true'
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Release Artifacts
        uses: softprops/action-gh-release@v2
        with:
          files: |
            frontend/dist/*.dmg
            frontend/dist/*.zip
            frontend/dist/*.exe
            frontend/dist/*.blockmap
